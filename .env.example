# Frontend (Vercel)
VITE_BACKEND_URL=

# Backend (Render)
POSTGRES_URL=
BACKEND_URL=
FRONTEND_URL=
CORS_ORIGINS=
PORT=4000
NODE_ENV=production
JWT_SECRET=
SESSION_SECRET=
DB_POOL_MAX=10

# Required only when running the secure seed command.
ADMIN_EMAIL=
ADMIN_PASSWORD=
USER_EMAIL=
USER_PASSWORD=